class Solution:
    def minMoves2(self, nums: List[int]) -> int:
        nums.sort()
        num = nums[len(nums)//2]
        print(f'num = {num}')
        res = 0
        for i in nums:
            res+= abs(i-num)
        return res
